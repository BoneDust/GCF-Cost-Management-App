import 'package:flutter/material.dart';

class ForeManReceiptScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      //TODO
      child: Text("Foreman Receipt"),
    );
  }
}
