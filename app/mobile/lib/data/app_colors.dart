import 'package:flutter/material.dart';

class AppColors {
 static final Color primaryColor = Colors.lightGreen;
 static final Color lightPrimaryColor =  Colors.green;
 static final Color darkPrimaryColor = Colors.green;
 static final Color textIconsColor = Colors.green;
 static final Color accentColor = Colors.greenAccent[400];
 static final Color primaryText = Colors.white;
 static final Color secondaryText = Colors.grey;
 static final Color divider = Colors.grey[300];
}